// Match all brackets
/\([^\(|^\)|.]*\)/g

// Match only first bracket
/\([^\(|^\)|.]*\)/

// Brackets will be included in the match
/\([^.]*\)/g        // ^. means any character that is not a '.'
/\(.*\)/g
