# Kernel Versions
# https://www.rackaid.com/blog/check-your-centos-version-number/
echo \>\>\> uname -r:
uname -r

echo \>\>\> lsb_release -a:
lsb_release -a

echo \>\>\> cat /proc/version:
cat /proc/version

echo \>\>\> cat /etc/issue:
cat /etc/issue

echo \>\>\> cat /etc/centos-release
cat /etc/centos-release
echo \>\>\> cat /etc/redhat-release
cat /etc/redhat-release